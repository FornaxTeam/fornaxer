import 'package:flutter/material.dart';

class DetailsConsoleView extends StatelessWidget {
  const DetailsConsoleView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Console'),
    );
  }
}
